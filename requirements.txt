# Expert-Level Local AI Agent - Dependencies
# وكيل الذكاء الاصطناعي المحلي المتقدم - المتطلبات

# Core dependencies
requests>=2.31.0
rich>=13.7.0

# Data analysis
pandas>=2.1.0
openpyxl>=3.1.2
xlrd>=2.0.1

# Web access
ddgs>=0.1.0  # New package name (preferred)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# System monitoring
psutil>=5.9.0

# Optional: Docker SDK (install separately if needed)
# docker>=7.0.0

# Optional: Kubernetes client (install separately if needed)
# kubernetes>=28.1.0

# Optional: SSH operations (install separately if needed)
# paramiko>=3.4.0

# Config file parsing
pyyaml>=6.0.1

# Security and encryption
cryptography>=41.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Desktop Application (PyQt6)
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0
PyQt6-sip>=13.6.0
QScintilla>=2.14.0  # For code editor syntax highlighting
markdown>=3.5.0  # For markdown rendering in chat