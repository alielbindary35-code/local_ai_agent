# دليل إدارة قاعدة المعرفة - Knowledge Base Management Guide

## 📚 عرض المعلومات المخزنة

### الطريقة السريعة:
```bash
python view_knowledge.py
```

هذا سيعرض:
- إحصائيات قاعدة المعرفة
- آخر 10 إدخالات
- التحقق من التكرار

### الطريقة التفاعلية:
```bash
python -m src.utils.knowledge_viewer
```

هذا يعطيك قائمة تفاعلية:
1. عرض جميع الإدخالات
2. عرض إدخال محدد (بالـ ID)
3. عرض الإحصائيات
4. البحث عن التكرار
5. الخروج

### من الكود:
```python
from src.utils.knowledge_viewer import KnowledgeViewer

viewer = KnowledgeViewer()

# عرض الإحصائيات
viewer.show_statistics()

# عرض آخر 20 إدخال
viewer.view_all_knowledge(limit=20)

# عرض إدخال محدد
viewer.view_entry(entry_id=12)

# البحث عن التكرار
viewer.show_duplicates(similarity_threshold=0.8)
```

## 🔍 البحث عن التكرار

النظام يتحقق تلقائياً من التكرار عند:
- تخزين معرفة جديدة
- استخدام `show_duplicates()`

**مثال:**
```python
viewer = KnowledgeViewer()
duplicates = viewer.find_duplicates(similarity_threshold=0.8)
viewer.show_duplicates()
```

**النتيجة:**
- يعرض جميع الإدخالات المتشابهة
- يظهر نسبة التشابه (0.0 - 1.0)
- يقترح حذف التكرار

## 📊 الإحصائيات المتاحة

```python
stats = viewer.get_statistics()
# Returns:
# - total_entries: إجمالي الإدخالات
# - by_category: عدد الإدخالات حسب الفئة
# - by_source: عدد الإدخالات حسب المصدر
# - most_used: أكثر الإدخالات استخداماً
# - average_confidence: متوسط الثقة
# - total_learned: إجمالي أحداث التعلم
```

## 🗑️ حذف إدخال

```python
from src.core.knowledge_base import KnowledgeBase

kb = KnowledgeBase()
# Note: حذف الإدخالات يتطلب إضافة method في KnowledgeBase
# يمكنك حذفها يدوياً من قاعدة البيانات
```

## 🔧 إصلاح تحذير DuckDuckGo

### الطريقة الصحيحة:
```bash
# تثبيت الحزمة الجديدة
pip install ddgs

# حذف الحزمة القديمة (اختياري)
pip uninstall duckduckgo-search
```

### التحقق:
```bash
pip list | grep -i duck
# يجب أن ترى فقط: ddgs
```

## 📍 موقع قاعدة البيانات

قاعدة البيانات موجودة في:
```
data/agent_memory.db
```

يمكنك فتحها باستخدام:
- SQLite Browser
- أي أداة SQLite
- من Python: `sqlite3 data/agent_memory.db`

## 🎯 أمثلة عملية

### مثال 1: عرض جميع معرفة Docker
```python
viewer = KnowledgeViewer()
viewer.view_all_knowledge(category="docker", limit=50)
```

### مثال 2: البحث عن تكرار في معرفة معينة
```python
viewer = KnowledgeViewer()
# البحث عن تكرار مع threshold منخفض (أكثر حساسية)
viewer.show_duplicates(similarity_threshold=0.7)
```

### مثال 3: عرض إدخال محدد
```python
viewer = KnowledgeViewer()
# عرض الإدخال رقم 12 (مثل المثال في السؤال)
viewer.view_entry(entry_id=12)
```

## 💡 نصائح

1. **فحص التكرار بانتظام**: استخدم `show_duplicates()` أسبوعياً
2. **مراقبة الثقة**: الإدخالات بثقة منخفضة (< 0.5) قد تحتاج مراجعة
3. **حذف القديم**: الإدخالات غير المستخدمة (usage_count = 0) يمكن حذفها
4. **النسخ الاحتياطي**: احفظ `agent_memory.db` بانتظام

## 🔗 الجداول في قاعدة البيانات

- `knowledge_entries`: الإدخالات الرئيسية
- `knowledge_patterns`: الأنماط المستخرجة
- `learning_history`: سجل التعلم
- `solutions`: الحلول (من Memory system)

## 📝 ملاحظات

- كل إدخال له ID فريد
- `usage_count` يزيد تلقائياً عند استخدام الإدخال
- `confidence` يحدد جودة المعرفة (0.0 - 1.0)
- `source` يحدد مصدر المعرفة (user_interaction, web_search, etc.)

